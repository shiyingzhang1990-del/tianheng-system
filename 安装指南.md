# 天衡系统 安装指南

## 系统要求

| 项目 | 最低要求 |
|------|---------|
| 操作系统 | Windows 10+ / macOS 11+ |
| Python | 3.10 及以上 |
| 磁盘空间 | 约 5GB（含 AI 模型） |
| 内存 | 4GB 以上 |
| 网络 | 首次安装需联网下载模型 |

## 快速安装（推荐）

### macOS 用户

1. 双击 `启动天衡系统.command` 文件
2. 首次运行会自动安装所有依赖（约 2-5 分钟）
3. 提示输入 DeepSeek API Key 时，粘贴你的密钥
4. 浏览器自动打开，使用 `admin / admin123` 登录

### Windows 用户

1. 双击 `启动天衡系统.bat` 文件
2. 首次运行会自动安装所有依赖（约 2-5 分钟）
3. 提示输入 DeepSeek API Key 时，粘贴你的密钥
4. 浏览器自动打开，使用 `admin / admin123` 登录

## 手动安装

如果自动脚本出现问题，可以手动执行以下步骤：

### 1. 安装 Python

- 打开 https://www.python.org/downloads/
- 下载最新版本（3.10+）
- **Windows 用户务必勾选 "Add Python to PATH"**

### 2. 创建虚拟环境并安装依赖

```bash
# macOS 终端执行：
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# Windows 命令提示符执行：
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 3. 下载 AI 模型（约 470MB）

```bash
python download_model.py
```

如果下载失败，手动下载模型并解压到 `models/embedding/` 目录：
https://hf-mirror.com/sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2

### 4. 配置 API 密钥

将 `.env.example` 复制为 `.env`，并填写你的 DeepSeek API Key：

```
DEEPSEEK_API_KEY=sk-你的密钥
```

获取密钥：https://platform.deepseek.com/

### 5. 启动

```bash
python app.py
```

浏览器访问 http://localhost:5001

## 常见问题

### Q: 双击脚本后提示"无法打开，因为无法验证开发者"

**Mac:** 系统设置 → 隐私与安全性 → 点击"仍要打开"

### Q: 模型下载失败怎么办？

1. 检查网络是否正常
2. 尝试设置环境变量：`HF_ENDPOINT=https://hf-mirror.com`
3. 手动下载模型文件放到 `models/embedding/` 目录

### Q: 端口 5001 被占用？

修改 `.env` 文件，添加 `PORT=5002`（或其他端口）

### Q: 如何修改管理员密码？

修改 `.env` 文件中的 `DEFAULT_PASSWORD`

### Q: 没有 DeepSeek API Key 能用吗？

可以启动，但问答功能会显示"演示模式"，没有 AI 智能回答。
需要去 https://platform.deepseek.com/ 注册获取（新用户有免费额度）。

### Q: 如何升级到新版本？

1. 备份 `uploads/`、`.env` 和 `instance/app.db` 文件
2. 替换所有项目文件
3. 恢复备份的三个文件/目录
4. 重新运行启动脚本

## 技术支持

如遇到问题，请联系软件提供商获取技术支持。
